import React from 'react';

const CustomSignInButton = () => {
  return (
    <button
      style={{
        backgroundColor: 'blue', // Change this to your desired background color
        color: 'white', // Change this to your desired text color
        borderRadius: '5px', // Add more styling as needed
        // Add any other CSS styles you want to customize the button
      }}
    >
      Sign In
    </button>
  );
};

export default CustomSignInButton;
